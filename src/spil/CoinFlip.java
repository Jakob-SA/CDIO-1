package spil;

public class CoinFlip {

    private int value;

    private void CoinFlip(){
        value = (int)Math.round(Math.random());
        if (value == 0)
            System.out.println();


        }
    }

